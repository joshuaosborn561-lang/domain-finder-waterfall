[project]
name = "domain-waterfall"
version = "0.1.0"
description = "Standalone MCP service: company name + location → trusted domain"
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
